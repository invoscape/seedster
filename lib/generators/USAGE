Description:
    This generator creates the files and folders needed for seedster

Example:
    rails generate seedster settings

    This will create:
        create 'db/seedster'
        create 'db/seedster/datasets.yml'
